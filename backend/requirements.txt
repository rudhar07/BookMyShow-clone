# --- Web framework ---
fastapi==0.115.6          # the API framework: async, type-driven, auto-docs at /docs
uvicorn[standard]==0.34.0 # the ASGI server that actually runs FastAPI

# --- Database ---
SQLAlchemy==2.0.36        # the ORM: lets us write Python classes instead of raw SQL

# --- Validation / settings ---
pydantic==2.10.4          # request/response validation (FastAPI uses it under the hood)

# Note: SQLite itself needs NO install — it ships inside Python's standard library.
# That is a big reason it's perfect for a project like this: zero setup, one file on disk.
